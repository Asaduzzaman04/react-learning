

const Data = () => {
    return (
        <>
            <p>this components this exported using default method</p>
        </>
    );
};

export default Data;

{/* this components is exported  by default method in single file we can export  once we can give any name the file name when import this file but in named export method we can't do it */}